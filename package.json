{
  "name": "daily-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:portal": "turbo run dev --filter=@daily/portal",
    "dev:life": "turbo run dev --filter=@daily/life",
    "dev:learning": "turbo run dev --filter=@daily/learning",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}
